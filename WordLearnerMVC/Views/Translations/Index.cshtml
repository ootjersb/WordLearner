@model IEnumerable<WordLearnerMVC.Translation>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Language.LanguageName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Word.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Meaning)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Language.LanguageName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Word.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Meaning)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TranslationID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TranslationID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TranslationID })
        </td>
    </tr>
}

</table>
